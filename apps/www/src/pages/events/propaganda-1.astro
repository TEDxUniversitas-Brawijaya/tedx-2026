---
import PaperTexture1 from "@/assets/imgs/paper-texture-1.png";
import Layout from "@/components/layout.astro";
import Navbar from "@/components/navbar.astro";
import { Image } from "astro:assets";
import House1 from "@/assets/imgs/house-1.png";
import GreenRoomBackground2 from "@/assets/imgs/green-room-background-2.png";
import Chandelier1 from "@/assets/imgs/chandelier-1.png";
import ClosedDoorSkewed from "@/assets/imgs/closed-door-skewed.png";
import Sofa2 from "@/assets/imgs/sofa-2.png";
import Table2 from "@/assets/imgs/table-2.png";
import Bench1 from "@/assets/imgs/bench-1.png";
import Tree1 from "@/assets/imgs/tree-1.png";
---

<Layout
  title="Selasar Penerimaan | TEDx Universitas Brawijaya"
  description="Pulang merupakan metafora dari rasa aman yang melampaui batas fisik sebuah bangunan; ia adalah kondisi emosional di mana seseorang merasa diterima seutuhnya, didengar tanpa rasa takut, dan dicintai tanpa ancaman penolakan. Rumah menjadi ruang konsisten yang tidak menggunakan kelemahan sebagai senjata atau membalas kegagalan dengan penghakiman. Di tengah dunia yang semakin cepat dan terfragmentasi, konsep pulang ini kemudian meluas menjadi sebuah kebutuhan untuk merancang teknologi, sistem, dan desain ruang yang mampu menghadirkan kembali ketenangan serta rasa keterhubungan yang menyembuhkan bagi manusia."
  canonical="https://tedxuniversitasbrawijaya.com/events/propaganda-1"
>
  <Navbar />
  <main>
    <div class="gradient-1"></div>

    <section id="hero-section">
      <div class="text-container">
        <h1>Selasar Penerimaan</h1>
        <p>
          Pulang merupakan metafora dari rasa aman yang melampaui batas fisik
          sebuah bangunan; ia adalah kondisi emosional di mana seseorang merasa
          diterima seutuhnya, didengar tanpa rasa takut, dan dicintai tanpa
          ancaman penolakan. Rumah menjadi ruang konsisten yang tidak
          menggunakan kelemahan sebagai senjata atau membalas kegagalan dengan
          penghakiman. Di tengah dunia yang semakin cepat dan terfragmentasi,
          konsep pulang ini kemudian meluas menjadi sebuah kebutuhan untuk
          merancang teknologi, sistem, dan desain ruang yang mampu menghadirkan
          kembali ketenangan serta rasa keterhubungan yang menyembuhkan bagi
          manusia.
        </p>
      </div>
      <Image src={House1} alt="" class="house-1" />
    </section>
    <section id="explanation-section">
      <div class="explanation-wrapper">
        <div id="horizontal-scroll">
          <Image
            src={GreenRoomBackground2}
            alt=""
            class="green-room-background-2"
          />
          <div class="green-room-background-2-things">
            <Image src={Chandelier1} alt="" class="chandelier-1" />
            <Image src={Chandelier1} alt="" class="chandelier-2" />
            <div class="closed-door-skewed-container">
              <Image src={ClosedDoorSkewed} alt="" class="closed-door-skewed" />
            </div>
            <Image src={Sofa2} alt="" class="sofa-2" />
            <Image src={Table2} alt="" class="table-2" />
          </div>
          <div id="first-screen">
            <div class="text-container">
              <h2>Menanggalkan Asing</h2>
              <p>
                Menceritakan tentang momen saat kita sampai di depan pintu rumah
                setelah seharian menghadapi dunia yang melelahkan. Di luar sana,
                kita sering kali terpaksa berpura-pura kuat atau menjadi orang
                lain hanya agar bisa diterima dan dihargai. Begitu sampai di
                rumah, ambang pintu menjadi tempat kita "meletakkan" semua beban
                dan sandiwara itu. Kita tidak lagi perlu memakai topeng; kita
                bisa melepas rasa lelah, berhenti bersikap waspada, dan kembali
                menjadi diri sendiri yang apa adanya—tanpa takut dihakimi atau
                ditolak.
              </p>
            </div>
          </div>
          <div id="second-screen">
            <div class="text-container">
              <h2>Ruang Tumbuh</h2>
              <p>
                Membahas tentang pentingnya rasa aman (secure attachment)
                sebagai fondasi karakter seseorang. Rumah diibaratkan sebagai
                "laboratorium aman" di mana kita pertama kali belajar mencoba,
                melakukan kesalahan, dan mengalami kegagalan. Jika di dalam
                rumah setiap kegagalan kita disambut dengan pelukan, bukan
                kemarahan atau penolakan, maka kita akan tumbuh dengan keyakinan
                bahwa jatuh bukanlah akhir dari segalanya. Rasa aman inilah yang
                nantinya kita bawa ke dunia luar; karena kita tahu ada tempat
                pulang yang selalu menerima, kita pun berubah menjadi pribadi
                yang lebih berani mengambil risiko, lebih tangguh, dan tidak
                takut menghadapi tantangan hidup.
              </p>
            </div>
          </div>
          <div id="third-screen">
            <div class="outside-background"></div>
          </div>
          <div id="fourth-screen">
            <div class="outside-things">
              <div class="tree-1-container">
                <Image src={Tree1} alt="" class="tree-1 tree-1-shadow" />
                <Image src={Tree1} alt="" class="tree-1" />
              </div>
              <Image src={Bench1} alt="" class="bench-1" />
            </div>
            <div class="text-container">
              <h2>Memahat Tenang</h2>
              <p>
                Berbicara tentang seni bertahan hidup di tengah dunia modern
                yang berjalan sangat cepat dan penuh gangguan. Saat ini, kita
                sering merasa "asing" dan kehilangan arah meski sedang berada di
                tengah keramaian, karena rasa aman dan ketenangan tidak lagi
                datang secara otomatis. Oleh karena itu, kita harus secara sadar
                "memahat" atau membangun sendiri ruang dan kebiasaan yang bisa
                menghadirkan kembali rasa tenang tersebut. Baik melalui desain
                ruang yang nyaman, penggunaan teknologi yang bijak, maupun
                menjaga hubungan yang berkualitas, kita sedang menciptakan
                sistem perlindungan agar jiwa kita tetap memiliki tempat untuk
                pulang dan beristirahat di tengah dunia yang kacau.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
    <Image src={PaperTexture1} alt="" class="paper-texture-1" />
  </main>
</Layout>
<style>
  main {
    background-color: var(--color-black);
    position: relative;
  }
  .gradient-1 {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 130dvh;
    background: linear-gradient(to top, #0b4848 20%, #f8f1dc 70%);
    pointer-events: none;
  }
  #hero-section {
    height: 100dvh;
    width: 100dvw;
    position: relative;
    overflow: hidden;
    padding-top: var(--navbar-height);
  }
  #hero-section .text-container {
    position: absolute;
    top: 20%;
    left: 50%;
    transform: translate(-50%, 0%);
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }
  #hero-section .text-container h1 {
    font-family: var(--font-serif);
    font-size: var(--text-l);
    color: var(--color-black);
  }
  #hero-section .text-container p {
    font-family: var(--font-sans);
    font-size: var(--text-s);
    color: var(--color-black);
    width: 90dvw;
    text-wrap-style: pretty;
  }
  #hero-section .house-1 {
    position: absolute;
    left: -30%;
    bottom: -5%;
    width: 150dvw;
    max-width: none;
    height: auto;
    object-fit: cover;
    object-position: center;
    pointer-events: none;
    aspect-ratio: 1.08 / 1;
    transform: scaleX(-1);
    z-index: 20;
    mask-image:
      linear-gradient(
        to right,
        transparent 5%,
        black 25%,
        black 75%,
        transparent 95%
      ),
      linear-gradient(
        to bottom,
        transparent 5%,
        black 50%,
        black 50%,
        transparent 95%
      );
    mask-composite: intersect;
  }
  #explanation-section {
    height: 700dvh;
    position: relative;
  }
  #explanation-section .explanation-wrapper {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    height: 100dvh;
    overflow: hidden;
    background-color: var(--color-green);
  }
  #explanation-section .explanation-wrapper .green-room-background-2 {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 250dvw;
    max-width: none;
    height: calc(100dvh - var(--navbar-height));
    object-fit: cover;
    object-position: right center;
    pointer-events: none;
    aspect-ratio: 5.43 / 1;
    mask-image: linear-gradient(to bottom, transparent 0%, black 10%);
    mask-composite: intersect;

    z-index: 0;
  }
  #explanation-section
    .explanation-wrapper
    .green-room-background-2-things
    .chandelier-1 {
    position: absolute;
    top: var(--navbar-height);
    left: 30%;
    width: auto;
    max-width: none;
    height: 25dvh;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 0.97 / 1;
  }
  #explanation-section
    .explanation-wrapper
    .green-room-background-2-things
    .chandelier-2 {
    position: absolute;
    top: var(--navbar-height);
    left: 170%;
    width: auto;
    max-width: none;
    height: 25dvh;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 0.97 / 1;
  }
  #explanation-section
    .explanation-wrapper
    .green-room-background-2-things
    .closed-door-skewed-container {
    position: absolute;
    bottom: 0;
    left: 250dvw;
    transform: translate(-31%, 0%);
    width: auto;
    height: 60dvh;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 0.78 / 1;
    z-index: 20;
  }
  #explanation-section
    .explanation-wrapper
    .green-room-background-2-things
    .closed-door-skewed {
    position: absolute;
    bottom: 0;
    left: 0;
    width: auto;
    height: 100%;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 0.25 / 1;
  }
  #explanation-section
    .explanation-wrapper
    .green-room-background-2-things
    .sofa-2 {
    position: absolute;
    bottom: 5%;
    left: 0%;
    transform: translate(-50%, 0%);
    width: auto;
    max-width: none;
    height: 30dvh;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 2.29 / 1;
  }
  #explanation-section
    .explanation-wrapper
    .green-room-background-2-things
    .table-2 {
    position: absolute;
    bottom: 2%;
    left: 65%;
    width: auto;
    max-width: none;
    height: 30dvh;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 1.71 / 1;
  }
  #explanation-section .explanation-wrapper #horizontal-scroll {
    position: relative;
    height: 100dvh;
    width: 100%;
    display: flex;
    flex-shrink: 0;
    overflow: visible;
    transform: translate(var(--translate-x), 0%);
  }
  #explanation-section .explanation-wrapper #first-screen {
    height: 100dvh;
    width: 100dvw;
    padding-top: var(--navbar-height);
    flex-shrink: 0;
    position: relative;
  }
  #explanation-section .explanation-wrapper #first-screen .text-container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }
  #explanation-section .explanation-wrapper #first-screen .text-container h2 {
    font-family: var(--font-serif);
    font-size: var(--text-m);
    color: var(--color-white);
  }
  #explanation-section .explanation-wrapper #first-screen .text-container p {
    font-family: var(--font-sans);
    font-size: var(--text-s);
    color: var(--color-white);
    width: 90dvw;
    text-wrap-style: pretty;
  }
  #explanation-section .explanation-wrapper #second-screen {
    height: 100dvh;
    width: 100dvw;
    padding-top: var(--navbar-height);
    flex-shrink: 0;
    position: relative;
  }
  #explanation-section .explanation-wrapper #second-screen .text-container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }
  #explanation-section .explanation-wrapper #second-screen .text-container h2 {
    font-family: var(--font-serif);
    font-size: var(--text-m);
    color: var(--color-white);
  }
  #explanation-section .explanation-wrapper #second-screen .text-container p {
    font-family: var(--font-sans);
    font-size: var(--text-s);
    color: var(--color-white);
    width: 90dvw;
    text-wrap-style: pretty;
  }
  #explanation-section .explanation-wrapper #third-screen {
    height: 100dvh;
    width: 100dvw;
    padding-top: var(--navbar-height);
    flex-shrink: 0;
    position: relative;
    display: block;
  }
  #explanation-section .explanation-wrapper #third-screen .outside-background {
    position: absolute;
    top: 0;
    left: 50%;
    width: 50%;
    height: 100%;
    background-color: var(--color-white);
  }
  #explanation-section .explanation-wrapper #fourth-screen {
    height: 100dvh;
    width: 100dvw;
    padding-top: var(--navbar-height);
    flex-shrink: 0;
    position: relative;
    background-color: var(--color-white);
    overflow: hidden;
  }
  #explanation-section .explanation-wrapper #fourth-screen .text-container {
    position: absolute;
    top: 20%;
    left: 50%;
    transform: translate(-50%, 0%);
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }
  #explanation-section .explanation-wrapper #fourth-screen .text-container h2 {
    font-family: var(--font-serif);
    font-size: var(--text-m);
    color: var(--color-black);
  }
  #explanation-section .explanation-wrapper #fourth-screen .text-container p {
    font-family: var(--font-sans);
    font-size: var(--text-s);
    color: var(--color-black);
    width: 90dvw;
    text-wrap-style: pretty;
  }
  #explanation-section .explanation-wrapper .outside-things {
    position: absolute;
    top: 0;
    right: 0;
    width: 100dvw;
    height: 100dvh;
  }
  #explanation-section .explanation-wrapper .outside-things .bench-1 {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translate(-50%, 0%);
    width: auto;
    height: 20dvh;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 1.74 / 1;
  }
  #explanation-section .explanation-wrapper .outside-things .tree-1-container {
    position: absolute;
    bottom: 0;
    right: 0;
    transform: translate(50%, 0%);
    width: auto;
    height: 50dvh;
    object-fit: cover;
    pointer-events: none;
    aspect-ratio: 0.92 / 1;
    mask-composite: intersect;
  }
  #explanation-section .explanation-wrapper .outside-things .tree-1 {
    position: absolute;
    bottom: 0;
    right: 0;
    width: auto;
    height: 100%;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 0.73 / 1;
    mask-image: linear-gradient(to right, transparent 10%, black 50%);
    mask-composite: intersect;
  }
  #explanation-section .explanation-wrapper .outside-things .tree-1-shadow {
    position: absolute;
    bottom: 0;
    right: 0;
    transform: skew(30deg, 0deg) translate(-120%, 0%);
    width: auto;
    height: 50%;
    object-fit: contain;
    pointer-events: none;
    aspect-ratio: 0.73 / 1;
    filter: saturate(0) blur(5px);
    opacity: 0.3;
  }
  .paper-texture-1 {
    position: fixed;
    top: calc(0% + var(--navbar-height));
    left: 50%;
    width: 200dvw;
    height: 200dvh;
    object-fit: cover;
    pointer-events: none;
    mix-blend-mode: multiply;
    aspect-ratio: 1.78 / 1;
    transform-origin: top center;
    transform: rotate(0deg) translate(-50%, 0%);
  }

  @media (min-width: 425px) {
    #hero-section .house-1 {
      left: -15%;
      bottom: -5%;
      width: 100dvw;
    }
  }

  @media (min-width: 768px) {
    #hero-section .house-1 {
      left: -5%;
      bottom: -15%;
      width: 50dvw;
    }
    #hero-section .text-container {
      top: 30%;
      gap: 2.5rem;
    }
    #hero-section .text-container h1 {
      font-size: var(--text-xl);
    }
    #hero-section .text-container p {
      font-size: var(--text-m);
      width: 60dvw;
    }
    #explanation-section {
      height: 600dvh;
    }
    #explanation-section .explanation-wrapper .green-room-background-2 {
      width: 220dvw;
    }
    #explanation-section
      .explanation-wrapper
      .green-room-background-2-things
      .chandelier-1 {
      top: var(--navbar-height);
      left: 30%;
      width: 15dvw;
    }
    #explanation-section
      .explanation-wrapper
      .green-room-background-2-things
      .chandelier-2 {
      top: var(--navbar-height);
      left: 170%;
      width: 15dvw;
    }
    #explanation-section
      .explanation-wrapper
      .green-room-background-2-things
      .closed-door-skewed-container {
      height: 80dvh;
      left: 220dvw;
      transform: translate(-31%, 0%);
    }
    #explanation-section
      .explanation-wrapper
      .green-room-background-2-things
      .sofa-2 {
      bottom: 0;
      left: -5%;
      width: auto;
      height: 35dvh;
    }
    #explanation-section
      .explanation-wrapper
      .green-room-background-2-things
      .table-2 {
      bottom: 0;
      left: 85%;
      width: auto;
      height: 40dvh;
    }
    #explanation-section .explanation-wrapper #first-screen .text-container {
      top: 30%;
      gap: 2.5rem;
      transform: translate(0%, 0%);
    }
    #explanation-section .explanation-wrapper #first-screen .text-container h2 {
      font-size: var(--text-l);
    }
    #explanation-section .explanation-wrapper #first-screen .text-container p {
      font-size: var(--text-m);
      width: 40dvw;
    }
    #explanation-section .explanation-wrapper #second-screen .text-container {
      top: 40%;
      gap: 2.5rem;
    }
    #explanation-section
      .explanation-wrapper
      #second-screen
      .text-container
      h2 {
      font-size: var(--text-l);
    }
    #explanation-section .explanation-wrapper #second-screen .text-container p {
      font-size: var(--text-m);
      width: 40dvw;
    }
    #explanation-section .explanation-wrapper #third-screen {
      width: 40dvw;
    }
    #explanation-section .explanation-wrapper #fourth-screen {
      width: 60dvw;
    }
    #explanation-section .explanation-wrapper #fourth-screen .text-container {
      top: 20%;
      gap: 2.5rem;
      transform: translate(-50%, 0%);
    }
    #explanation-section
      .explanation-wrapper
      #fourth-screen
      .text-container
      h2 {
      font-size: var(--text-l);
    }
    #explanation-section .explanation-wrapper #fourth-screen .text-container p {
      font-size: var(--text-m);
      width: 40dvw;
    }
    #explanation-section .explanation-wrapper .outside-things {
      width: 100%;
    }
    #explanation-section .explanation-wrapper .outside-things .bench-1 {
      bottom: 0;
      left: 50%;
      transform: translate(-50%, 0%);
      width: auto;
      height: 30dvh;
    }
    #explanation-section
      .explanation-wrapper
      .outside-things
      .tree-1-container {
      bottom: 0;
      right: 0;
      transform: translate(30%, 0%);
      width: auto;
      height: 80dvh;
    }
    #explanation-section .explanation-wrapper .outside-things .tree-1 {
      mask-image: linear-gradient(to right, transparent 10%, black 80%);
    }
  }
</style>
<script>
  (() => {
    const explanationSection = document.getElementById("explanation-section");
    const horizontalScroll = document.getElementById("horizontal-scroll");

    if (!(explanationSection && horizontalScroll)) {
      return;
    }

    const animateParagraphs = () => {
      const sectionRect = explanationSection.getBoundingClientRect();
      const sectionHeight = explanationSection.offsetHeight;
      const viewportHeight = window.innerHeight;

      // Calculate scroll progress (0 to 1)
      const scrollProgress = Math.max(
        0,
        Math.min(1, -sectionRect.top / (sectionHeight - viewportHeight))
      );

      // Easing functions for smooth animations
      const easeInOutCubic = (t: number): number => {
        return t < 0.5 ? 4 * t * t * t : 1 - (-2 * t + 2) ** 3 / 2;
      };

      function transform(
        x: number,
        input: number[],
        output: number[],
        easing: (t: number) => number = (t) => t
      ) {
        const firstInput = input[0];
        const lastInput = input.at(-1);
        const lastOutput = output.at(-1);

        if (
          !(
            firstInput !== undefined &&
            lastInput !== undefined &&
            lastOutput !== undefined
          )
        ) {
          return 0;
        }

        const clampedX = Math.max(firstInput, Math.min(lastInput, x));

        for (let i = 0; i < input.length - 1; i++) {
          const currentInput = input[i];
          const nextInput = input[i + 1];

          const currentOutput = output[i];
          const nextOutput = output[i + 1];

          if (
            !(
              currentInput !== undefined &&
              nextInput !== undefined &&
              currentOutput !== undefined &&
              nextOutput !== undefined
            )
          ) {
            continue;
          }

          if (clampedX >= currentInput && clampedX <= nextInput) {
            const localProgress =
              (clampedX - currentInput) / (nextInput - currentInput);
            const easedProgress = easing(localProgress);

            return currentOutput + easedProgress * (nextOutput - currentOutput);
          }
        }

        return lastOutput;
      }

      const horizontalScrollX = transform(
        scrollProgress,
        [0, 1],
        innerWidth < 768 ? [0, -300] : [0, -200],
        easeInOutCubic
      );
      horizontalScroll.style.setProperty(
        "--translate-x",
        `${horizontalScrollX}dvw`
      );
    };

    window.addEventListener("scroll", animateParagraphs);
    animateParagraphs(); // Initial call
  })();
</script>
